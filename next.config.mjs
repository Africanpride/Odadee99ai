/** @type {import('next').NextConfig} */
const nextConfig = {
    // Add imajge source
    images: {
        domains: ['images.unsplash.com', 'images.pexels.com'],
        },
};

export default nextConfig;
